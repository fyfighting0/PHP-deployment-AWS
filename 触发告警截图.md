# 触发告警截图

## 1. RDS 每日 Dump 备份

在 Cluster 中可直接触发执行任务进行测试，成功触发通知：

![alt text](images/PG备份成功.png)

通过设置不存在的 S3 桶名称触发执行任务，成功触发告警：

![alt text](images/PG备份失败.png)

## 2. GitHub CI/CD

在 master 中 push 或者 merge 到 master，成功触发通知：

![alt text](images/CICD成功.png)

通过 push 一个有 bug 的 Dockerfile，成功触发部署失败告警：

![alt text](images/CICD失败.png)

## 3. AppConfig

修改 AppConfig 的环境变量，触发重新部署，成功触发通知：

![alt text](images/APPCONFIG同步ECS成功.png)

通过设置不对的 ECS 路径与名字，成功触发部署失败告警：

![alt text](images/APPCONFIG同步ECS失败.png)

## 4. 日志报警

### ERROR 告警

使用 AWS CLI 注入日志方法或直接在 UI 上创建一个带有 ERROR 的 LOG 触发告警：

![alt text](images/error日志报警.png)

### Load Balancer 的 500 比例高于 10% 告警

在 Laravel 项目中，增加一个路由进行测试，让它直接抛出 500 异常。在浏览器多次刷这个接口，从而触发告警。

![alt text](images/lb500超过10报警.png)
